# office (VBWD Office) runtime dependencies.
#
# openpyxl (S147-4) — .xlsx read/write for VBWD Spreadsheets import/export.
# Optional at the CODE level (sheet_import_export.py raises
# OfficeSheetUnavailableFormatError -> a clean 501 if this is ever absent at
# runtime, never a silent, lossy xlsx path) but declared here so it IS
# installed in every environment that runs this plugin — the image installs
# each plugin's own requirements.txt, never a plugin's ad-hoc pip call.
openpyxl==3.1.5

# python-docx (S147-3 toolbar bundle) — .docx export for VBWD Docs, rendered
# from the JSON content model (never from stored HTML, epic D7). PDF export
# reuses the CORE PdfService/WeasyPrint seam instead of a new dependency.
python-docx==1.1.2

# Spreadsheets PDF export (S147-4 drag/toolbar slice) needs no new
# dependency: it renders an HTML table through the CORE PdfService/
# WeasyPrint seam already used above by VBWD Docs' own PDF export — see
# sheet_editor_service.py's OfficeSheetEditorService._render_pdf.
