"""VBWD Office — source package.

Phase 0 (S147-00) ships plumbing and empty states only: no document model, no
upload, no share, no editor. ``storage/``, ``models/``, ``repositories/`` are
deliberately empty seams that S147-1 (VBWD Space — vault, folders, upload,
download, versions, quota) fills in.
"""
